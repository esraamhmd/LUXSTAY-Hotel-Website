
services:
  luxstay:
    build:
      context: .
      dockerfile: Dockerfile
    image: luxstay:latest
    container_name: luxstay
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    env_file:
      - .env
    restart: unless-stopped